html
    head
        meta(charset='UTF-8')
        meta(http-equiv="X-UA-Compatible",content="IE=edge")
        meta(name="viewport",content="width=device-width, initial-scale=1")
        link(href='/css/bootstrap.css', rel='stylesheet')
        link(href='/css/style.css', rel='stylesheet')
        title
            if (title)
                | #{title}
            else
                | new site
    body
        nav(class="navbar navbar-expand-md navbar-dark bg-dark")
            div.navbar-header
                button(type="button", class="navbar-toggler", data-toggle="collapse", data-target="#header-nav")
                    span.navbar-toggler-icon
                a(class="navbar-brand", href="/")  My Project
            div(id="navbar", class="collapse navbar-collapse")
                ul(class="nav navbar-nav")
                    li.nav-item
                        a(class="nav-link", href="/dashboard/home") Home
                    li.nav-item
                        a(class="nav-link", href="/dashboard/login") Login
                    li.nav-item
                        a(class="nav-link", href="/dashboard/register") Register

        div.container.mt-5
            block content

        script(src='/js/jquery_331.js')
        script(src='/js/bootstrap.js')
        script(src='/js/script.js')